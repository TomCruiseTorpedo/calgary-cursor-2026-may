# Server
PORT=3000

# OpenRouter — optional AI polish on /request (toggle in UI)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/free
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_APP_NAME=Plain Jane's Task Ask

# Optional: ECC (local Cursor harness — .cursor/ is gitignored)
# ECC_HOOK_PROFILE=standard
# ECC_DISABLED_HOOKS=pre:observe:continuous-learning,post:observe:continuous-learning
